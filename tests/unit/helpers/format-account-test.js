import { formatAccount } from 'gustav0/helpers/format-account';
import { module, test } from 'qunit';

module('Unit | Helper | format account');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatAccount([42]);
  assert.ok(result);
});
