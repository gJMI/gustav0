import { formatIban } from 'gustav0/helpers/format-iban';
import { module, test } from 'qunit';

module('Unit | Helper | format iban');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatIban([42]);
  assert.ok(result);
});
