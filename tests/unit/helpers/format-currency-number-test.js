import { formatCurrencyNumber } from 'gustav0/helpers/format-currency-number';
import { module, test } from 'qunit';

module('Unit | Helper | format currency number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatCurrencyNumber([42]);
  assert.ok(result);
});
