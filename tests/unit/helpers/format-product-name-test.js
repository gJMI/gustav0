import { formatProductName } from 'gustav0/helpers/format-product-name';
import { module, test } from 'qunit';

module('Unit | Helper | format product name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatProductName([42]);
  assert.ok(result);
});
